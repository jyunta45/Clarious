STEP 1 — Convert Memory Into Guidance (CRITICAL FIX)
❌ Old behavior (problem)

Assistant reads summary and says:

"You are between jobs… fighting addiction loops… launching business…"

This feels copied + robotic.

✅ New rule

Memory becomes internal steering only.

Add this function:

function extractGuidance(summary) {
  if (!summary) return "";

  return `
Use past session context only to guide tone,
priority, and relevance.

DO NOT repeat personal facts.
DO NOT list remembered situations.
DO NOT summarize the user back to themselves.
Respond only to the current message.
`;
}
✅ STEP 2 — Inject Guidance Instead of Raw Memory

Where you previously injected session memory:

❌ REMOVE
messages.push({
  role: "system",
  content: session.summary
});
✅ REPLACE WITH
messages.push({
  role: "system",
  content: extractGuidance(session.summary)
});

Now memory influences reasoning silently.

Huge psychological improvement.

✅ STEP 3 — Present-Moment Response Rule

Add permanent instruction:

const PRESENT_RULE = `
Speak as if meeting the user in this moment.
Never sound like recalling stored knowledge.
Focus on helping forward movement.
`;

Inject once:

messages.push({
  role: "system",
  content: PRESENT_RULE
});
✅ STEP 4 — Grounding Question Trigger

(The Next-Level Improvement)

Add detector:

function needsGroundingQuestion(userMessage) {
  const triggers = [
    "difference",
    "better",
    "why",
    "compare",
    "unique",
    "chatgpt",
    "gemini"
  ];

  const msg = userMessage.toLowerCase();
  return triggers.some(t => msg.includes(t));
}
✅ STEP 5 — Grounding Question Generator
function groundingQuestion() {
  const questions = [
    "What decision are you thinking through right now?",
    "What feels most unclear for you today?",
    "What are you trying to move forward on?",
    "Where do you feel stuck lately?",
    "What matters most for you right now?"
  ];

  return questions[
    Math.floor(Math.random() * questions.length)
  ];
}
✅ STEP 6 — Inject Question (Interaction Loop)

After assistant reply:

if (needsGroundingQuestion(userMessage)) {
  assistantReply += "\n\n" + groundingQuestion();
}
✅ STEP 7 — Prevent Over-Asking (Important)
if (
  session.lastGrounding &&
  Date.now() - session.lastGrounding < 180000
){
  return assistantReply;
}

session.lastGrounding = Date.now();

Max once every 3 minutes.

Natural pacing.