Please upgrade my AI life assistant app with the following 
improvements. Read ALL existing files first before making 
any changes. Do not break existing functionality.

---

1. UPGRADE adaptiveDepth.js — Replace complexity detection with this:

function detectComplexity(message) {
  const text = message.toLowerCase().trim();
  const wordCount = text.split(/\s+/).length;
  let score = 0;

  if (wordCount < 12) return "LOW";

  const strongSignals = [
    "i don't know what", "should i quit",
    "important choice", "no hope",
    "losing myself", "don't see the point",
    "feels pointless", "not sure who i am",
  ];

  const mediumSignals = [
    "part of me", "can't seem to",
    "i keep", "been thinking about",
    "feeling lost", "feeling confused",
    "fear of", "pressure to",
    "getting worse", "keeps coming back",
    "career decision", "wrong direction",
    "what if i", "my future", "stuck in"
  ];

  for (const s of strongSignals) {
    if (text.includes(s)) {
      score += 2;
      if (score >= 3) return "HIGH";
    }
  }

  for (const s of mediumSignals) {
    if (text.includes(s)) {
      score += 1;
      if (score >= 3) return "HIGH";
    }
  }

  if (wordCount > 80) score += 2;
  if (wordCount > 140) score += 1;

  return score >= 3 ? "HIGH" : "LOW";
}

Add this model router in the same file:

function selectModel(complexity) {
  return complexity === "HIGH"
    ? "gpt-4.1"
    : "gpt-4.1-mini";
}

Export both functions.

---

2. UPGRADE continuityEngine.js — Add this memory system:

Memory object structure:
{
  goals: [],
  recurringStruggles: [],
  strengths: [],
  decisionPatterns: [],
  identityDirection: "",
  lastUpdated: null
}

Add deduplication guard:
function addUnique(arr, value) {
  if (!arr.some(v => v.toLowerCase() === value.toLowerCase())) {
    arr.push(value);
  }
}

Memory update trigger — only run when ALL three are true:
1. complexity === "HIGH"
2. session.memoryUpdatedThisSession is not true
3. memory.lastUpdated is older than 6 hours

When updating memory:
- Use model "gpt-4.1-mini" to extract facts
- Extract only explicitly revealed facts. Never infer.
- Prompt must return JSON only:
  {
    "goal": null,
    "recurringStruggle": null,
    "strength": null,
    "decisionPattern": null,
    "identityDirection": null
  }
- Use null for anything not clearly expressed

When saving to PostgreSQL user_data table:
- MERGE with existing memory. Never replace arrays.
- Append only through addUnique()
- Never overwrite existing goals, struggles, strengths, 
  or decisionPatterns arrays
- Only replace identityDirection if new value is not null
- Update lastUpdated to Date.now()

After update set session.memoryUpdatedThisSession = true.

---

3. UPGRADE contextBuilder.js — Add memory injection:

Before each AI call, build memory context:

function buildMemoryContext(memory) {
  if (!memory.identityDirection && memory.goals.length === 0)
    return "";

  const goals = memory.goals.slice(-3).join(", ");
  const struggles = memory.recurringStruggles.slice(-2).join(", ");

  return `
User is working toward: ${memory.identityDirection || "direction still forming"}
Current focus: ${goals || "exploration"}
Recurring friction: ${struggles || "none consistently observed"}
`.trim();
}

Append memory block to system prompt when available:
const memoryBlock = buildMemoryContext(userMemory);
const finalPrompt = memoryBlock
  ? SYSTEM_PROMPT + "\n\n" + memoryBlock
  : SYSTEM_PROMPT;

Keep under 60 tokens. Never inject empty memory.

---

4. UPGRADE capabilityLayer.js — Replace system prompt with:

const SYSTEM_PROMPT = `
You are an adaptive life assistant designed to help users
think clearly, regain stability, and move forward intelligently.

Your purpose is not to make decisions for users,
but to strengthen their judgment and capability.

Before replying, silently determine:

1. Situation
   Recovery | Decision | Understanding | Execution | Celebration

2. Cognitive Need
   Stabilize | Clarify | Evaluate | Act | Acknowledge

3. Leadership Calibration
   Confused    → supportive
   Evaluating  → collaborative
   Hesitating  → gently persuasive
   Urgent      → calm and direct

Core Principles:
- Match response depth to user effort.
- When users explain deeply, slow internal reasoning before answering.
- Reflect underlying dilemmas when present.
- Help users understand implications and possible future outcomes.
- Emphasize conditions that allow progress rather than certainty.
- Offer perspectives users may not have considered.
- Preserve full user ownership of decisions.
- Avoid generic motivation or empty reassurance.
- Do not rush complex situations.
- Execution guidance should remain simple and practical.
- Maintain continuity with previously known user goals
  when relevant, without repeating or summarizing memory.
- If stored memory conflicts with the user's current statement,
  prioritize the present moment over stored memory.
  Humans evolve.

Style:
- Calm, grounded, intelligent.
- Human but composed.
- Persuasive without pressure.
- Natural language over technical wording.
- Speak like a capable thinking partner.

High-complexity responses should:
- Acknowledge tension accurately.
- Clarify thinking before suggesting action.
- Restore sense of capability.
- Reduce overwhelm without minimizing reality.

Silence Rule:
If few words are sufficient, use few words.
Do not elaborate to appear thorough.

Close with a short forward-moving statement when appropriate.
Calm. Precise. Non-motivational.
`;

---

5. UPGRADE server.js — Add cost protection:

Daily reset — call at start of every request:
function resetDailyUsage(user) {
  const today = new Date().toDateString();
  if (user.lastUsageDate !== today) {
    user.dailyTokens = 0;
    user.softWarnedToday = false;
    user.efficiencyMode = false;
    user.lastUsageDate = today;
  }
}

Input truncation — run before every AI call:
function truncateInput(input) {
  const MAX_CHARS = 3200;
  if (input.length > MAX_CHARS) {
    return { text: input.slice(0, MAX_CHARS), truncated: true };
  }
  return { text: input, truncated: false };
}
If truncated === true, prepend to system prompt:
"Your message was long — I'm focusing on the core of what you shared."

Output token limits:
function maxTokens(complexity, efficiencyMode = false) {
  if (efficiencyMode) return 180;
  if (complexity === "LOW") return 300;
  if (complexity === "HIGH") return 600;
  return 300;
}

Daily budget protection:
const DAILY_LIMIT = 135000;

function checkBudget(user, complexity) {
  if (user.efficiencyMode) {
    return {
      model: "gpt-4.1-mini",
      efficiencyMode: true,
      systemNotice: "Running in efficiency mode for today."
    };
  }
  if (user.dailyTokens > DAILY_LIMIT * 0.80 && !user.softWarnedToday) {
    user.softWarnedToday = true;
    return {
      model: selectModel(complexity),
      systemNotice: "You're having a deep day. I'll keep responses focused."
    };
  }
  if (user.dailyTokens > DAILY_LIMIT) {
    user.efficiencyMode = true;
    return {
      model: "gpt-4.1-mini",
      efficiencyMode: true,
      systemNotice: "Running in efficiency mode for today."
    };
  }
  return { model: selectModel(complexity) };
}

Session timeout rule:
If user inactive more than 10 minutes:
- Archive conversation into session_summary field
- Clear message history only
- DO NOT clear memory, identity data, or user profile
- DO NOT interrupt active streaming

Store these fields in user_data table using existing 
Drizzle ORM (merge, never overwrite):
- dailyTokens
- softWarnedToday
- efficiencyMode
- lastUsageDate

---

CRITICAL RULES FOR AGENT:
- Read ALL existing files before making any changes
- Preserve all existing features:
  authentication, streaming, habits, mood tracking,
  identity layers, all existing routes
- Use existing Drizzle ORM and PostgreSQL for all storage
- Never use placeholder model names like "fast-model"
  Always use "gpt-4.1" or "gpt-4.1-mini" explicitly
- Merge memory data, never overwrite arrays
- Do not modify frontend index.html unless necessary
- Do not change database schema unless absolutely required

After completing all upgrades, verify:
- Server starts successfully
- AI response still streams correctly
- Authentication still works
- Existing database data is intact
- Memory merges correctly without overwriting